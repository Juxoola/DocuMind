# CUDA-сборки torch (cu128) — устанавливаются поверх pyproject.toml
# Используется: pip install -r requirements-cuda.txt
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.8.0+cu128
torchaudio==2.8.0+cu128
torchvision==0.23.0+cu128
