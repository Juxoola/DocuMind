# ── Опциональные зависимости: PyTorch-режим эмбеддингов/реранкера ──
#
# Этот файл нужен ТОЛЬКО если вы НЕ используете GGUF-модели для эмбеддингов
# и реранкера. Дефолтная конфигурация (Qwen3-Embedding-0.6B-v2.Q8_0.gguf
# + Qwen3-Reranker-0.6B-v2.Q8_0.gguf) работает БЕЗ этих зависимостей —
# llama-server в папке bin/ обслуживает модели через OpenAI-совместимый API.
#
# Устанавливайте этот файл ТОЛЬКО если в config.py:
#   EMBEDDING_MODEL_NAME = "Qwen/Qwen3-Embedding-0.6B"  (не .gguf)
#   или
#   RERANKER_MODEL_NAME = "Qwen/Qwen3-Reranker-0.6B"    (не .gguf)
#
# Установка:
#   pip install -r requirements-pt-extras.txt
#
# Размер на диске: ~4-5 ГБ (transformers + bitsandbytes + torchvision).

# --- Quantization для HuggingFace-эмбеддингов ---
transformers
bitsandbytes

# --- torchvision (для некоторых vision-моделей через transformers) ---
torchvision
